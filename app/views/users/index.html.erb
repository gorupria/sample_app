<h1>All users</h1>
<p id ="total_users">Total Users: <%= @users.count %></p>
<%= will_paginate %>
<ul class="users">
	<!-- #is in 1004, timestamp: 38:00, very imp refactoring check -->	
	<%= render @users %>
</ul>
<%= will_paginate %>