<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
  <!--at:1002, timestamp:12:00 -->
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', :f => f %>
  <div class="actions">
  	<%= f.submit "Update" %>
  </div>

  <div>
  	<%= gravatar_for @user %>
    <a href="http://gravatar.com/emails" target=_blank>change</a>
  </div>
<% end %>
